# ==========================
# = SERVER
# ==========================

# server properties
server.port=8081
server.compression.enabled=true


# ==========================
# = SPRING APPLICATION
# ==========================

# application's name
spring.application.name=psw-project


# ==========================
# = SPRING SECURITY
# ==========================

# OAuth2 server
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/psw-realm
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/realms/psw-realm/protocol/openid-connect/certs


# ==========================
# = KEYCLOAK
# ==========================

# client name
keycloak.resource=myclient


# ==========================
# = SPRING DATASOURCE
# ==========================

# datasource properties
spring.datasource.password=jec3adah
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.url=jdbc:mysql://localhost:3306/nutrizione_db?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC


# ==========================
# = SPRING JPA
# ==========================

# database platform
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect